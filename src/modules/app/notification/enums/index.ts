export enum NotificationPriority {
  LOW = 'low',
  NORMAL = 'normal',
  HIGH = 'high',
  URGENT = 'urgent',
}

export enum NotificationMedium {
  EMAIL = 'email',
  SMS = 'sms',
  PUSH = 'push',
  PORTAL = 'portal',
}
